#include <stdio.h>

int main() {
    int t1, t2, t3;
    double time;

    printf("Введіть три значення: ");
    scanf("%d %d %d", &t1, &t2, &t3);

    time = 1.0 / (1.0 / t1 + 1.0 / t2 + 1.0 / t3);

    printf("Необхідний час: %.2f год.\n", time);

    return 0;
}